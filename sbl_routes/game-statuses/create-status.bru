meta {
  name: Create Game Status
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/game-statuses
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "à jouer"
  }
}
