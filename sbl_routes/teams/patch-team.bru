meta {
  name: Patch Team
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/teams/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Team Alpha Patched"
  }
}
