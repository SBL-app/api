meta {
  name: Patch Player
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/players/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Player One Patched"
  }
}
